{
  "name": "@radatek/mqtt",
  "version": "1.2.5",
  "description": "MQTT Broker and Client",
  "author": "Darius Kisonas",
  "license": "MIT",
  "keywords": [
    "mqtt",
    "broker",
    "mqtt-broker",
    "mqtt-client"
  ],
  "type": "module",
  "main": "mqtt.ts",
  "repository": {
    "type": "git",
    "url": "https://github.com/radateklt/mqtt.git"
  },
  "scripts": {
    "build": "pnpm --package typescript dlx tsc -d -m nodenext -t es2023 mqtt.ts --stripInternal --outDir dist && sed -E ':a;N;$!ba;s|\"main\":\\s*\"([^\"]+)\\.ts\"[^\\n]*|\"main\": \"\\1.js\",\\n  \"module\": \"\\1.js\",\\n  \"types\": \"\\1.d.ts\",|;s|,\\n  \"scripts\".*$|\\n}|' package.json > dist/package.json && cp README.md dist/",
    "test": "node test.ts",
    "prepublishOnly": "pnpm build"
  },
  "publishConfig": {
    "directory": "dist",
    "access": "public"
  },
  "devDependencies": {
    "@types/node": "^25.3.3"
  }
}
